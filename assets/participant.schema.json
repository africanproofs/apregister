{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/africanproofs/apregister/main/assets/participant.schema.json",
  "title": "Flare Participant Metadata",
  "description": "Schema for participant.json — off-chain metadata for Flare ecosystem participants registered via the ParticipantRegister contract. Current canonical structure uses `flare:networks` for per-chain service breakdown; the flat `flare:services` array is retained for backward compatibility but deprecated. Infrastructure is split into two arrays: `flare:nodes` for operational hardware (where the participant physically runs nodes — implicitly internal), and `flare:tools` for public ecosystem services the participant publishes for the community (RPCs, dashboards, explorers, faucets, etc.). The legacy `flare:rpc` array is retained for backward compatibility but deprecated.",
  "type": "object",
  "required": ["@context", "@type", "name", "url"],
  "properties": {
    "@context": {
      "type": "object",
      "description": "JSON-LD context. Must include Schema.org vocab and flare namespace."
    },
    "@type": {
      "type": "string",
      "const": "Organization"
    },
    "name": {
      "type": "string",
      "maxLength": 64,
      "description": "Display name of the participant."
    },
    "description": {
      "type": "string",
      "maxLength": 350,
      "description": "Short description of the participant and their services."
    },
    "url": {
      "type": "string",
      "maxLength": 256,
      "format": "uri",
      "description": "Website URL."
    },
    "logo": {
      "type": "string",
      "maxLength": 256,
      "format": "uri",
      "description": "Direct URL to the primary logo image. Universal fallback — consumers use this when no theme-specific or square variant is available."
    },
    "flare:brand": {
      "type": "object",
      "description": "Theme-aware and shape-specific logo variants. All optional. Consumers fall back to top-level `logo` if absent.",
      "properties": {
        "light": {
          "type": "string",
          "maxLength": 256,
          "format": "uri",
          "description": "Logo variant for light backgrounds (dark mark/text on transparent)."
        },
        "dark": {
          "type": "string",
          "maxLength": 256,
          "format": "uri",
          "description": "Logo variant for dark backgrounds (light mark/text on transparent)."
        },
        "icon": {
          "type": "string",
          "maxLength": 256,
          "format": "uri",
          "description": "Square mark / icon (recommended 512x512, symbol only). Used for avatars, directory tiles, favicons."
        }
      },
      "additionalProperties": false
    },
    "flare:social": {
      "type": "object",
      "description": "Social media usernames (not full URLs).",
      "properties": {
        "twitter": { "type": "string", "maxLength": 64 },
        "telegram": { "type": "string", "maxLength": 64 },
        "discord": { "type": "string", "maxLength": 64 },
        "github": { "type": "string", "maxLength": 64 }
      },
      "additionalProperties": false
    },
    "flare:location": {
      "type": "object",
      "description": "Organization domicile.",
      "properties": {
        "country": {
          "type": "string",
          "pattern": "^[A-Z]{2}$",
          "description": "ISO 3166-1 alpha-2 country code."
        }
      },
      "additionalProperties": false
    },
    "flare:services": {
      "type": "array",
      "description": "DEPRECATED — use `flare:networks` for per-chain service breakdown. This flat list is retained for backward compatibility with pre-2026-05-09 participant.json files. New JSONs should use `flare:networks` instead.",
      "items": {
        "type": "string",
        "enum": ["ftso", "fdc", "fast-updates", "validator", "fassets-agent", "lending", "dex", "staking"]
      }
    },
    "flare:networks": {
      "type": "array",
      "description": "Networks the participant operates on, with per-network service breakdown. Current canonical structure (supersedes the flat `flare:services` array). Each entry pairs a chain id with the services the participant offers on that chain.",
      "items": {
        "type": "object",
        "required": ["id", "services"],
        "properties": {
          "id": {
            "type": "string",
            "enum": ["flare", "songbird", "coston2", "coston"],
            "description": "Chain identifier the entry applies to."
          },
          "services": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": ["ftso", "fdc", "fast-updates", "validator", "fassets-agent", "lending", "dex", "staking"]
            }
          }
        },
        "additionalProperties": false
      }
    },
    "flare:participant-type": {
      "type": "integer",
      "minimum": 0,
      "maximum": 7,
      "description": "Numeric ParticipantType enum value the wallet registered under, mirroring the on-chain (participantType, infoURI) tuple. MUST equal the value used in register(). Lets JSON consumers verify the document matches the on-chain registration without an additional RPC call. Enum: 0=Provider, 1=DeFi, 2=Wallet, 3=Tool, 4=FAssetsAgent, 5=Exchange, 6=App, 7=AgenticAI."
    },
    "flare:participant-type-label": {
      "type": "string",
      "maxLength": 32,
      "enum": ["Provider", "DeFi", "Wallet", "Tool", "FAssets Agent", "Exchange", "App", "Agentic AI"],
      "description": "Human-readable mirror of flare:participant-type for indexers and UIs that prefer string keys. MUST match the numeric value (Provider↔0, DeFi↔1, Wallet↔2, Tool↔3, FAssets Agent↔4, Exchange↔5, App↔6, Agentic AI↔7)."
    },
    "flare:nodes": {
      "type": "array",
      "description": "Infrastructure node declarations. Declares where nodes run physically.",
      "items": {
        "type": "object",
        "required": ["network", "role", "country"],
        "properties": {
          "network": {
            "type": "string",
            "enum": ["flare", "songbird", "coston2", "coston"]
          },
          "role": {
            "type": "string",
            "enum": ["validator", "observer", "fdc-verifier", "rpc", "indexer"]
          },
          "country": {
            "type": "string",
            "pattern": "^[A-Z]{2}$",
            "description": "ISO 3166-1 alpha-2 country code where the node physically runs."
          }
        },
        "additionalProperties": false
      }
    },
    "flare:tools": {
      "type": "array",
      "description": "Public ecosystem services the participant publishes for the community. Always public, always has a URL. Distinct from `flare:nodes` which declares operational hardware. Use for RPCs, block explorers, dashboards, indexers, faucets, bridges, subgraphs, analytics surfaces, dev tooling, educational sites — anything an external user can browse to or call.",
      "items": {
        "type": "object",
        "required": ["name", "url", "category"],
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 64,
            "description": "Display name of the tool (e.g. \"AU.CC Flare RPC\", \"Flare Builders\")."
          },
          "url": {
            "type": "string",
            "format": "uri",
            "maxLength": 256,
            "description": "Public URL where the tool is reachable. MUST be https://."
          },
          "category": {
            "type": "string",
            "enum": [
              "rpc",
              "explorer",
              "indexer",
              "archive",
              "dashboard",
              "faucet",
              "bridge",
              "subgraph",
              "analytics",
              "dev-tool",
              "educational",
              "other"
            ],
            "description": "Kind of public service. `rpc` = JSON-RPC endpoint; `explorer` = block explorer; `indexer` = indexer/API over chain data; `archive` = archive-node endpoint; `dashboard` = web dashboard/observability surface; `faucet` = testnet faucet; `bridge` = cross-chain bridge UI; `subgraph` = hosted subgraph endpoint; `analytics` = analytics/metrics surface; `dev-tool` = developer tooling (SDK docs, deployer UI, etc.); `educational` = educational/docs site; `other` = catch-all."
          },
          "networks": {
            "type": "array",
            "description": "Chains the tool serves. Omit when the tool is chain-agnostic (e.g. an educational site).",
            "items": {
              "type": "string",
              "enum": ["flare", "songbird", "coston2", "coston"]
            }
          },
          "description": {
            "type": "string",
            "maxLength": 200,
            "description": "Optional one-line description of the tool."
          }
        },
        "additionalProperties": false
      }
    },
    "flare:rpc": {
      "type": "array",
      "description": "DEPRECATED — use `flare:tools` with `category: \"rpc\"` for public RPC endpoints. This array is retained for backward compatibility with pre-2026-05-13 participant.json files. New JSONs should use `flare:tools` instead.",
      "items": {
        "type": "object",
        "required": ["network", "url"],
        "properties": {
          "network": {
            "type": "string",
            "enum": ["flare", "songbird", "coston2", "coston"]
          },
          "url": {
            "type": "string",
            "format": "uri",
            "maxLength": 256
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": true
}
